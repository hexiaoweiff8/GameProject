using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using UnityEditor;
using UnityEngine;

[CustomEditor(typeof(NGUIHTML))]
class NGUIHtmlInspector : Editor
{
    NGUIHTML model;
    public override void OnInspectorGUI()
    {
        base.DrawDefaultInspector();

       // model = target as NGUIHTML;
        
        /*
        bool loop = EditorGUILayout.ToggleLeft(new GUIContent("循环"), model.IsLoop);
        if (loop != model.IsLoop)
            model.IsLoop = loop;
         */
        //UIAtlas a;
        //a.GetSprite
        //NGUIEditorTools.DrawAdvancedSpriteField(mAtlas, sprite.name, SelectSprite, true);
    }
}
 