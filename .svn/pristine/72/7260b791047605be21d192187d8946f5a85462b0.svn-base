-- region *.lua
-- Date
-- 此文件由[BabeLua]插件自动生成
local class = require("common/middleclass")
local skill_UI = class("skill_UI", ui_session)

function skill_UI.show_me()
    local sd = ui_session_data(ui_session_type.FIXED, ui_session_id.UI_skill_UI)
    m_ui_manager:show_session(skill_UI(sd))
end

function skill_UI:initialize(session_data)
    -- self._base.init(self, session_data)
    ui_session.initialize(self, session_data)
    self.session_id = ui_session_id.UI_skill_UI
end

function skill_UI:on_post_load()
    --    local left_btn = self.transform:Find("leftBtn").gameObject
    --    self.lua_behaviour:AddClick(left_btn, function(go)
    --    end )
end

function skill_UI:reset_window(args)

end

function skill_UI:on_pre_destroy()
    print("skill_UI:on_pre_destroy" .. self.gameObject.name)
end

return skill_UI