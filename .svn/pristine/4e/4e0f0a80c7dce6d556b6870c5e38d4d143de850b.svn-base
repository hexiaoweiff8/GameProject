using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

using UnityEditor;
using UnityEngine;
/*
[CustomEditor(typeof(PlayMovie))]
class PlayMovieInspector : Editor
{
	PlayMovie model;
	public override void OnInspectorGUI()
	{
		base.DrawDefaultInspector();

		model = target as PlayMovie;

        bool loop = EditorGUILayout.ToggleLeft(new GUIContent("循环"), model.IsLoop);
        if (loop != model.IsLoop)
            model.IsLoop = loop;


		if (GUILayout.Button ("播放", GUILayout.Width (100)))
			model.Play ();
 
		if (GUILayout.Button ("暂停", GUILayout.Width (100)))
			model.Pause ();

		
		if (GUILayout.Button ("停止", GUILayout.Width (100)))
			model.Stop ();


	}
}*/