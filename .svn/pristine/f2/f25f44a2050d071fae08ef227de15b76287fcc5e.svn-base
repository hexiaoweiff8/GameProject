-- region *.lua
-- Date
-- 此文件由[BabeLua]插件自动生成
local class = require("common/middleclass")
local ui_shopScene = class("ui_shopScene", ui_session)

function ui_shopScene.show_me()
    local sd = ui_session_data(ui_session_type.FIXED, ui_session_id.UI_shopScene)
    m_ui_manager:show_session(ui_shopScene(sd))
end

function ui_shopScene:initialize(session_data)
    ui_session.initialize(self, session_data)
    self.session_id = ui_session_id.UI_shopScene
end

function ui_shopScene:on_post_load()
    local bagLabel = self.transform:Find("Win/bagLabel").gameObject
    self.lua_behaviour:AddClick(bagLabel, function(go)
        m_ui_manager:get_session(ui_session_id.UI_inventory2):show_session(true)
    end )
end

function ui_shopScene:reset_window(args)

end

function ui_shopScene:on_pre_destroy()
    print("ui_shopScene:on_pre_destroy" .. self.gameObject.name)
end

return ui_shopScene