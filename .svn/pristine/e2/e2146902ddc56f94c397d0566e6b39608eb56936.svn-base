-- region *.lua
-- Date
-- 此文件由[BabeLua]插件自动生成
local class = require("common/middleclass")
local Tooltip = class("Tooltip", ui_session)

function Tooltip.show_me()
    local sd = ui_session_data(ui_session_type.FIXED, ui_session_id.UI_Tooltip)
    m_ui_manager:show_session(Tooltip(sd))
end

function Tooltip:initialize(session_data)
    -- self._base.init(self, session_data)
    ui_session.initialize(self, session_data)
    self.session_id = ui_session_id.UI_Tooltip
end

function Tooltip:on_post_load()
    --    local left_btn = self.transform:Find("leftBtn").gameObject
    --    self.lua_behaviour:AddClick(left_btn, function(go)
    --    end )
end

function Tooltip:reset_window(args)

end

function Tooltip:on_pre_destroy()
    print("Tooltip:on_pre_destroy" .. self.gameObject.name)
end

return Tooltip